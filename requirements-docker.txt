# V2 后端 Docker 镜像依赖清单。
# 与根 requirements.txt 内容一致，唯一差异：镜像基于 Python 3.11，
# esda 必须固定 <2.10（2.10+ 要求 Python>=3.12）。

# Web 框架
flask>=3.0
sqlalchemy>=2.0

# 数据处理与空间分析（V2 七阶段流水线）
pandas>=2.0
numpy>=1.24
scipy>=1.11
shapely>=2.0
pyproj>=3.6
geopandas>=0.14
esda>=2.9,<2.10     # Getis-Ord Gi*（2.10+ 要求 Python>=3.12，镜像用 3.11；G_Local API 稳定）
libpysal>=4.7       # 空间权重（KNN）

# HTTP / LLM 调用
requests>=2.31

# 导入导出
openpyxl>=3.1
python-docx>=0.8.11

# 可视化与 V1 兼容层（backend 内仍有 import，不可省）
matplotlib>=3.7
pillow>=10.0
networkx>=3.0
folium>=0.16
